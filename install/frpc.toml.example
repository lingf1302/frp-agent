# Replace these values, then start the service with:
# sudo systemctl enable --now frp-agent.service
serverAddr = "CHANGE_ME"
serverPort = 7000

# Uncomment for token authentication.
# auth.method = "token"
# auth.token = "CHANGE_ME"

# Add [[proxies]] entries below.
